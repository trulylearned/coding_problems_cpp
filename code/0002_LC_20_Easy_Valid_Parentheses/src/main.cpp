#include "solution.h"
#include <iostream>
#include <string>

// This is just a simple main to illustrate usage and for manual testing/debugging.
// It is not needed beyond manual debugging, that's why the code is commented out.
//
// To run tests, execute the following from the VS Code command palette:
// 1) CMake: Configure
// 2) CMake: Build
// 3) CTest: Run Tests
//
// After building, you can also run tests from the terminal. 
// Navigate to the 'build' directory and run `ctest --output-on-failure`.
//
// To see the Command Palette in VS Code, use the shortcut: 
// - Shift+Cmd+P (on Mac)
// - Shift+Ctrl+P (on Windows/Linux)

int main() {
    // Solution solution;
    //
    // std::string s = "([{}])";
    // bool result = solution.isValid(s);
    //
    // std::cout << "Is the string \"" << s << "\" valid? " 
    //           << (result ? "Yes" : "No") << std::endl;
    //
    // std::string s2 = "([)]";
    // bool result2 = solution.isValid(s2);
    //
    // std::cout << "Is the string \"" << s2 << "\" valid? " 
    //           << (result2 ? "Yes" : "No") << std::endl;

    return 0;
}