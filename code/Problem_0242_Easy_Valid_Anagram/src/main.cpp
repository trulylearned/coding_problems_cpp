#include "solution.h"
#include <iostream>
#include <string>

// This is just a simple main to illustrate usage and manual testing and debugging.
// It is not needed beyond manual debugging, that's why the code is commented out.
//
// To run tests, execute the following from the VS Code command palette:
// 1) CMake: Configure
// 2) CMake: Build
// 3) CTest: Run Tests
//
// After building, you can also run tests from the terminal. 
// From the root folder, run:
// ctest --test-dir build --output-on-failure

int main() {
    // Solution solution;
    //
    // std::string s1 = "anagram";
    // std::string t1 = "nagaram";
    //
    // bool result1 = solution.is_anagram(s1, t1);
    // std::cout << std::boolalpha 
    //           << "Is '" << t1 << "' an anagram of '" << s1 << "': " 
    //           << result1 << std::endl; // Expected: true
    //
    // std::string s2 = "rat";
    // std::string t2 = "car";
    //
    // bool result2 = solution.is_anagram(s2, t2);
    // std::cout << std::boolalpha 
    //           << "Is '" << t2 << "' an anagram of '" << s2 << "': " 
    //           << result2 << std::endl; // Expected: false
    //
    return 0;
}