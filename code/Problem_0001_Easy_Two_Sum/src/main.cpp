#include "solution.h"
#include <iostream>
#include <vector>

// This `main` function is for demonstrating and debugging the solution.
// To run the automated tests, use the CTest functionality in your IDE or
// from the command line in the `build` directory: ctest --output-on-failure
int main() {
    /*
    // Example usage:
    Solution solution;
    std::vector<int> nums = {2, 7, 11, 15};
    int target = 9;

    std::vector<int> result = solution.two_sum(nums, target);

    if (!result.empty()) {
        std::cout << "Indices found: " << result[0] << ", " << result[1] << std::endl;
    } else {
        std::cout << "No solution found." << std::endl;
    }
    */
    return 0;
}